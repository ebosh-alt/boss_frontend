import UserContext from "../../utils/contexts";
import {useContext} from "react";

function AdvertisementAbout(photos, ) {
    const {userId, setUserId} = useContext(UserContext);

    return (
        <div>
        </div>
    );
}

export default AdvertisementAbout;
